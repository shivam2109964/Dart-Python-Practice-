import 'dart:io';

/// [Try-Catch] (Exception Handling)
/// Write a program that:
/// Takes two input: numerator and denominator
/// Performs divison
/// Uses [try-catch] to handle the cases when denominator is 0
/// Prints
/// ["Result is 5"] (if Valid)
/// [Cannot divide by zero] (if denominator is 0)
/// your task
/// Use [Try-catch]

void main() {
  print("Enter first numbers");
  int number = int.parse(s)  
}
